<style>
  :root{
    --g-footer-mid-bg:{{section.settings.footermidbg}};
    --g-footer-mid-text:{{section.settings.footermidtext}};
    --g-footer-mid-link:{{section.settings.footermidlink}};
    --g-footer-mid-title :{{section.settings.footermidtitle}};
    --g-footer-bot-border:{{section.settings.footerbotborder}};
    --g-footer-top-border:{{section.settings.footermidborder}};
    
    }
</style>
<style>
.footer-container{
  background: var(--g-footer-mid-bg);
  color: var(--g-footer-mid-text);
  border-top: 1px solid var(--g-footer-top-border);
}
  .footer-mid {
	border-top:1px solid var(--g-footer-bot-border);
}

.footer-mid a {
	color: var(--g-footer-mid-link)
}

.footer-mid a:hover {
	opacity: .85
}

.footer-bot {
	background: var(--g-footer-mid-bg);
	padding-top: 40px;
	padding-bottom: 65px
}


.footer-container {
	margin: 0 auto
}

.rtl .footer-container {
	text-align: right
}


.footer-mid__social li {
	width: 40px;
	height: 40px;
	line-height: 40px;
	border-radius: 50%;
	margin: 8px
}

.footer-top{
  padding: {{ section.settings.padding }}px 0px;
}

.footer__newsletter {
	max-width: 780px;
}
.footer__newsletter h3{
  color: var(--g-footer-mid-title);
}
</style>

<!-- footer-4.liquid -->
<div class="footer-container">
  
  <section class="footer-top text-center px-3">
    {%- if section.settings.newsletter -%}
    <div class="footer__newsletter mx-auto">
      {%- if section.settings.newsletter_title != blank -%}
      <h3 class="mb-3">
        {{ section.settings.newsletter_title }}
      </h3>
      {%- endif -%}
      {%- if section.settings.newsletter_subheading != blank -%}
      <p>{{ section.settings.newsletter_subheading }}</p>
      {%- endif -%}
      <div class="form-vertical pt-4">
        {%- form 'customer' -%}
        {{ form.errors | default_errors }}
        {%- if form.posted_successfully? -%}
        <p class="form-success">{{ 'general.newsletter_form.confirmation' | t }}</p>
        {%- else -%}
        <input type="hidden" name="contact[tags]" value="newsletter">
        <div class="input-group form-group">
          <input class="form-control py-4" type="email" value="{% if customer %}{{ customer.email }}{% endif %}" placeholder="{{ 'general.newsletter_form.newsletter_placeholder' | t }}" name="contact[email]" id="NewsletterEmail-{{ block.id }}" autocorrect="off" autocapitalize="off">
          <div class="input-group-append pl-3">
            <button type="submit" class="btn btn-theme gradient-theme" name="commit">
              {{ 'general.newsletter_form.submit' | t }}
            </button>
          </div>
        </div>
        {%- endif -%}
        {%- endform -%}
      </div>
    </div>
    {%- endif -%}
    {%- if section.settings.social_enable -%}
  <div class="footer-mid__social pt-1 ">
    <ul class="list--inline social-icons d-inline-block px-2 position-relative ">
      {%- if settings.social_facebook_link != blank -%}
      <li>
        <a href="{{ settings.social_facebook_link | escape }}" title="{{ 'layout.footer.social_platform' | t: name: shop.name, platform: 'Facebook' }}" style="color:#3b5999">
          {%- include 'icon-facebook' -%}</a>
      </li>
      {%- endif -%}
      {%- if settings.social_twitter_link != blank -%}
      <li>
        <a href="{{ settings.social_twitter_link | escape }}" title="{{ 'layout.footer.social_platform' | t: name: shop.name, platform: 'Twitter' }}" style="color:#55acee">
          {%- include 'icon-twitter' -%}</a>
      </li>
      {%- endif -%}
      {%- if settings.social_pinterest_link != blank -%}
      <li>
        <a href="{{ settings.social_pinterest_link | escape }}" title="{{ 'layout.footer.social_platform' | t: name: shop.name, platform: 'Pinterest' }}" style="color:#bd081c">
          {%- include 'icon-pinterest' -%}</a>
      </li>
      {%- endif -%}
      {%- if settings.social_tumblr_link != blank -%}
      <li>
        <a href="{{ settings.social_tumblr_link | escape }}" title="{{ 'layout.footer.social_platform' | t: name: shop.name, platform: 'Tumblr' }}" style="color:#34465d">
          {%- include 'icon-tumblr' -%}</a>
      </li>
      {%- endif -%}
      {%- if settings.social_snapchat_link != blank -%}
      <li>
        <a href="{{ settings.social_snapchat_link | escape }}" title="{{ 'layout.footer.social_platform' | t: name: shop.name, platform: 'Snapchat' }}" style="color:#FFFC00">
          {%- include 'icon-snapchat' -%}
        </a>
      </li>
      {%- endif -%}
      {%- if settings.social_youtube_link != blank -%}
      <li>
        <a href="{{ settings.social_youtube_link | escape }}" title="{{ 'layout.footer.social_platform' | t: name: shop.name, platform: 'YouTube' }}" style="color:#cd201f">
          {%- include 'icon-youtube' -%}
          </a>
      </li>
      {%- endif -%}
      {%- if settings.social_instagram_link != blank -%}
      <li>
        <a href="{{ settings.social_instagram_link | escape }}" title="{{ 'layout.footer.social_platform' | t: name: shop.name, platform: 'Instagram' }}" style="color:#e4405f">
          {%- include 'icon-instagram' -%}</a>
      </li>
      {%- endif -%}
      {%- if settings.social_vimeo_link != blank -%}
      <li>
        <a href="{{ settings.social_vimeo_link | escape }}" title="{{ 'layout.footer.social_platform' | t: name: shop.name, platform: 'Vimeo' }}" style="color:#1ab7ea">
          {%- include 'icon-vimeo' -%}
        </a>
      </li>
      {%- endif -%}
    </ul>
  </div>
  {%- endif -%}
  </section>
  
  <section class="footer-mid pt-3 pb-4  pt-lg-5 pb-lg-5 ">
    <div class="container-fluid">
      <div class="row align-items-lg-center">
        <div class="col-lg-4 col-md-12 text-center text-lg-left">
          {%- if section.settings.logo != blank -%}
          {%- assign logo = section.settings.logo -%}
          <div class="image-content__image-container logofooter mb-4 mb-lg-0" style="width: {{section.settings.widthlogofooter}}px;">
            <a class="image-content__image-wrapper" href="/" style="padding-top:{{ 1 | divided_by: logo.aspect_ratio | times: 100}}%;" >
              {%- assign img_url = logo | img_url: '1x1' | replace: '_1x1.', '_{width}x.' -%}
              <img 
              class="lazyload transition" 
              data-srcset="{{ img_url }}"
              data-widths="[360, 540, 720, 900, 1080, 1296, 1512, 1728, 2048]"
              data-aspectratio="{{ logo.aspect_ratio }}"
              data-sizes="auto"
              src="{%- include 'srcBlank' -%}"  />
            </a>
            
          </div>
          {%- endif -%}
          
        </div>
        <div class="col-lg-4 col-md-12 text-center">
          <div class="footer__powered">
            <p>&copy; Copyright {{ "now" | date: "%Y" }} <b>{{ shop.name }}</b>. {%- if section.settings.powered_enable -%}
              <span> {{ powered_by_link }}</span>
              {%- endif -%}</p>
          </div>
        </div>
      </div>
    </div>
  </section>
</div>
{% schema %}
{
  "name": "Footer 4",
  "settings": [
      {
        "type": "range",
        "id": "padding",
        "min": 0,
        "max": 200,
        "step": 10,
        "unit": "px",
        "label": "Padding Section",
        "default": 100
      },
      {
        "type": "header",
        "content": "Newsletter"
      },
      {
        "type": "checkbox",
        "id": "newsletter",
        "label": "Enable",
        "default": true
      },
      {
        "type": "text",
        "id": "newsletter_title",
        "label": "Heading",
        "default": "Be in the know"
      },
      {
        "type": "textarea",
        "id": "newsletter_subheading",
        "label": "Subheading",
        "default": "Promotions, new products and sales. Directly to your inbox."
      },
      {
        "type": "header",
        "content": "Mid footer"
      },
      {
        "type": "color",
        "id": "footermidbg",
        "label": "Background",
        "default": "#1e1e1e"
      },
      {
        "type": "color",
        "id": "footermidtitle",
        "label": "Text color",
        "default": "#000"
      },
      {
        "type": "color",
        "id": "footermidtext",
        "label": "Text color",
        "default": "#fff"
      },
      {
        "type": "color",
        "id": "footermidlink",
        "label": "Link color",
        "default": "#999"
      },
      {
        "type": "color",
        "id": "footermidborder",
        "label": "Border Top",
        "default": "#eee"
      },
      {
        "type": "color",
        "id": "footerbotborder",
        "label": "Border Bottom",
        "default": "#eee"
      },
      {
        "type": "image_picker",
        "id": "logo",
        "label": "Logo"
      },
      {
        "type": "range",
        "id": "widthlogofooter",
        "min": 0,
        "max": 300,
        "step": 10,
        "unit": "px",
        "label": "Padding Section",
        "default": 100
      },
      {
        "type": "checkbox",
        "id": "social_enable",
        "label": "Enable social",
        "default": true
      },
      
      {
        "type": "header",
        "content": "Copyright"
      },
      {
        "type": "checkbox",
        "id": "powered_enable",
        "label": "Enable Shopify powered",
        "default": true
      }
  ]
}
{% endschema %}